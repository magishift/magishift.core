{
  "name": "@magishift/keycloak",
  "version": "0.0.1",
  "description": "Magishift is Low-Code Framework, helps you to build application easier, faster and a more efficient use of time. (@keycloak)",
  "author": "Sofyan Hadi Ahmad",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/magishift/magishift.core"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc --p tsconfig.json -d && copyfiles -u -f -V src/**/*.graphql src/**/**/*.graphql dist",
    "precommit": "echo 'precommit'",
    "build:publish": "yarn build && yarn publish --no-git-tag-versions",
    "barrels": "barrelsby --delete -d ./src"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@magishift/config": "^0.0.3",
    "@magishift/http": "^0.0.3",
    "@magishift/redis": "^0.0.2",
    "@magishift/util": "^0.0.2",
    "ioredis": "^4.9.0",
    "jsonwebtoken": "^8.5.1",
    "keycloak-admin": "^1.12.0",
    "moment": "^2.24.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^6.1.1",
    "@nestjs/core": "^6.1.1",
    "@nestjs/swagger": "^3.0.2",
    "typeorm": "^0.2.16"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "@types/uuid": "^3.4.4",
    "barrelsby": "^2.0.0",
    "copyfiles": "^2.1.0",
    "nodemon": "^1.19.0",
    "supertest": "^4.0.2",
    "typescript": "^3.4.5"
  }
}
