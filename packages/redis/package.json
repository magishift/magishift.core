{
  "name": "@magishift/redis",
  "version": "0.0.5",
  "description": "Magishift is Low-Code Framework, helps you to build application easier, faster and a more efficient use of time. (@redis)",
  "author": "Sofyan Hadi Ahmad",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/magishift/magishift.core"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc --p tsconfig.json -d",
    "precommit": "echo 'precommit'",
    "build:publish": "yarn build && yarn publish --no-git-tag-version ",
    "barrels": "barrelsby --delete -d ./src"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@magishift/base": "^0.0.5",
    "@nestjs/common": "^6.3.1",
    "@nestjs/core": "^6.3.1",
    "ioredis": "^4.10.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^6.5.2",
    "typeorm": "^0.2.18",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.7",
    "@types/uuid": "^3.4.4",
    "nodemon": "^1.19.1",
    "supertest": "^4.0.2",
    "typescript": "^3.5.1"
  }
}
