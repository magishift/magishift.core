{
  "name": "@magishift/draft",
  "version": "0.0.1",
  "description": "Magishift is Low-Code Framework, helps you to build application easier, faster and a more efficient use of time. (@draft)",
  "author": "Sofyan Hadi Ahmad",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/magishift/magishift.core"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc --p tsconfig.json -d && copyfiles -u -f -V src/**/*.graphql src/**/**/*.graphql dist",
    "precommit": "echo 'precommit'",
    "build:publish": "yarn build && yarn publish"
  },
  "dependencies": {
    "@magishift/base": "^0.0.3",
    "@nestjs/common": "^6.1.1",
    "@nestjs/core": "^6.1.1",
    "@nestjs/typeorm": "^6.1.0",
    "typeorm": "^0.2.17"
  },
  "devDependencies": {
    "@types/node": "^11.13.9",
    "@types/uuid": "^3.4.4",
    "copyfiles": "^2.1.0",
    "nodemon": "^1.19.0",
    "supertest": "^4.0.2",
    "typescript": "^3.4.5"
  }
}
